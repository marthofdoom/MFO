native/build/
native/install/
native/out/
__pycache__/
*.pyc
*.zip
*.log
.vs/
compile_commands.json

# Third-party reference mods (method study only, never redistributed)
Docs/*.7z

# dotnet build artifacts (patcher)
installer/MFO.Synthesis/bin/
installer/MFO.Synthesis/obj/

# agent worktrees
.claude/worktrees/
# compiled fresh by tools/compile.sh at release (nondeterministic timestamp)
out/Scripts/MFO_Trade.pex

# Game binaries for offset/versionlib analysis — NEVER commit (copyrighted, large)
binaries/

# Partial binary-diff harness (Steamless + unpacked exes) — deferred 1.7.104 work, keep local
tools/binary-diff/
